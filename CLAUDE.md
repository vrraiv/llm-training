# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a new LLM training project. Update this section with the project's purpose, target model, and dataset details as the project develops.

## Setup

<!-- Add environment setup steps here, e.g.:
conda create -n llm-training python=3.11
conda activate llm-training
pip install -r requirements.txt
-->

## Commands

<!-- Add commands as the project is built out, e.g.:
| Task | Command |
|------|---------|
| Train | `python train.py --config configs/default.yaml` |
| Evaluate | `python eval.py --checkpoint checkpoints/latest` |
| Run tests | `pytest tests/` |
| Run single test | `pytest tests/test_foo.py::test_bar` |
| Lint | `ruff check .` |
| Format | `ruff format .` |
-->

## Architecture

<!-- Document the high-level structure once it exists, e.g.:
- `configs/` — YAML training configs (model, optimizer, data pipeline params)
- `data/` — Dataset loading, tokenization, and preprocessing
- `models/` — Model definitions or fine-tuning wrappers
- `train.py` — Main training loop entry point
- `eval.py` — Evaluation and metric computation
-->
